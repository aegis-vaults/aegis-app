# Make optional dependencies optional (fail gracefully)
optional=true

# Skip native build failures for optional dependencies
# This allows the build to continue even if hardware wallet USB dependencies fail
node-gyp-build-optional=true
